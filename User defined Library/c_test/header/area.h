/*  header file for area.c  */

# ifndef AREA_H
# define AREA_H

    double rectangle_area(double l, double b);
    double circle_area(double rad);
    double triangle_area(double base, double height);

#endif